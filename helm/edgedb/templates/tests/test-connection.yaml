apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "edgedb.fullname" . }}-test-connection"
  labels:
    {{- include "edgedb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "edgedb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
